import { EventsGrid } from '../components/EventsGrid';

/** Clean events listing (no marketing) — what the header "EVENTS" link points to. */
function EventsPage() {
	return <EventsGrid />;
}

export default EventsPage;
